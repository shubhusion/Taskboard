# Database connection string (PostgreSQL)
# For NeonDB: postgresql://user:password@host/database?sslmode=require
# For local PostgreSQL: postgresql://user:password@localhost:5432/taskboard
DATABASE_URL="your-postgres-connection-string"
